تمام 👌 خليني أشرحلك الموضوع خطوة بخطوة بشكل مبسط:

---

## 1️⃣ ما هو **i18n**؟

* i18n اختصار لـ **internationalization** (تدويل).
* مكتبة مشهورة في React هي **react-i18next**، وظيفتها تسهل علينا دعم **تعدد اللغات** في الموقع.
* بدل ما تكتب النصوص مباشرة داخل الكود، بتحطها في **ملفات ترجمة (JSON)** لكل لغة.
* المكتبة تختار الملف المناسب حسب اللغة الحالية وتعرض النصوص صح.

---

## 2️⃣ آلية العمل

* عندك ملف ترجمة للإنجليزي وملف للعربي.
* كل ملف عبارة عن **key → value**.

  * الـ key ثابت (زي معرف النص).
  * الـ value بتتغير حسب اللغة.

مثال:

```json
// en.json
{
  "welcome": "Welcome to our website",
  "login": "Login",
  "signup": "Sign Up"
}
```

```json
// ar.json
{
  "welcome": "مرحبًا بك في موقعنا",
  "login": "تسجيل الدخول",
  "signup": "إنشاء حساب"
}
```

---

## 3️⃣ إعداد i18n في React

ملف `i18n.js` (ممكن تحطه في جذر المشروع):

```jsx
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import en from "./locales/en.json";
import ar from "./locales/ar.json";

i18n
  .use(initReactI18next)
  .init({
    resources: {
      en: { translation: en },
      ar: { translation: ar },
    },
    lng: "en", // اللغة الافتراضية
    fallbackLng: "en", 
    interpolation: {
      escapeValue: false, 
    },
  });

export default i18n;
```

وبعدين في ملف `main.jsx` أو `index.js`:

```jsx
import "./i18n"; // لازم تستدعيه قبل أي component
```

---

## 4️⃣ استخدام الترجمة في الـ Components

في أي صفحة/كومبوننت:

```jsx
import { useTranslation } from "react-i18next";

export default function Home() {
  const { t } = useTranslation();

  return (
    <div>
      <h1>{t("welcome")}</h1>
      <button>{t("login")}</button>
      <button>{t("signup")}</button>
    </div>
  );
}
```

---

## 5️⃣ التبديل بين اللغتين

ممكن تعمل زر يغير اللغة:

```jsx
import { useTranslation } from "react-i18next";

export default function LangSwitcher() {
  const { i18n } = useTranslation();

  const toggleLang = () => {
    i18n.changeLanguage(i18n.language === "en" ? "ar" : "en");
  };

  return (
    <button 
      onClick={toggleLang} 
      className="bg-blue-500 px-4 py-2 rounded text-white"
    >
      {i18n.language === "en" ? "العربية" : "English"}
    </button>
  );
}
```

---

## 6️⃣ شكل **API الترجمة** (فقط كمثال)

لو الموقع عايز يجيب الترجمة من API بدل ملفات ثابتة:

* الـ backend يرجع JSON بناءً على اللغة المطلوبة.

🔹 **GET** `/api/translations?lang=en`

```json
{
  "welcome": "Welcome to our website",
  "login": "Login",
  "signup": "Sign Up"
}
```

🔹 **GET** `/api/translations?lang=ar`

```json
{
  "welcome": "مرحبًا بك في موقعنا",
  "login": "تسجيل الدخول",
  "signup": "إنشاء حساب"
}
```

---

✅ كدا تقدر:

1. تخزن النصوص لكل لغة.
2. تستخدم `t("key")` في أي مكان.
3. تبدل اللغة بزر واحد فقط.

تحب أجهزلك مثال كامل (ملفات `en.json` و `ar.json` + `i18n.js` + زر التبديل) عشان تبدأ بيه مشروعك مباشرة؟
