تمام ✨ خليني أشرحلك **AOS (Animate On Scroll)** بطريقة سهلة ونوريك إزاي تشتغل بيها مع **React.js** 👇

---

## 🔹 ما هي مكتبة AOS؟

* اختصار لـ **Animate On Scroll**.
* مكتبة خفيفة جدًا وظيفتها تعمل **Animations للعناصر** لما تظهر في الشاشة أثناء الـ Scroll.
* بتديك مؤثرات جاهزة زي: fade, zoom, flip, slide … إلخ.
* مش محتاجة كود JS كتير → مجرد تضيف **attributes** على العنصر.

---

## 🔹 تثبيت المكتبة مع React

في مشروع React:

```bash
npm install aos --save
```

وبعدها استدعيها في الكومبوننت الرئيسي (مثلاً `App.jsx`):

```jsx
import { useEffect } from "react";
import AOS from "aos";
import "aos/dist/aos.css";

function App() {
  useEffect(() => {
    AOS.init({
      duration: 1000, // مدة الأنيميشن (ms)
      easing: "ease-in-out", // نوع الحركة
      once: true, // الحركة تظهر مرة واحدة فقط
    });
  }, []);

  return (
    <div className="App">
      <h1 
        data-aos="fade-up" 
        className="text-3xl font-bold text-center mt-10"
      >
        Hello AOS 👋
      </h1>

      <p 
        data-aos="zoom-in" 
        className="text-center mt-5 text-gray-600"
      >
        هذا النص سيظهر مع أنيميشن عند التمرير
      </p>
    </div>
  );
}

export default App;
```

---

## 🔹 كيف نستخدمها؟

* تضيف على أي عنصر **Attribute** اسمه `data-aos` وتحدد نوع الأنيميشن.
* أمثلة:

  * `data-aos="fade-up"`
  * `data-aos="fade-down"`
  * `data-aos="zoom-in"`
  * `data-aos="flip-left"`
  * `data-aos="slide-right"`

مثال عملي:

```jsx
<div className="grid grid-cols-3 gap-5 p-10">
  <div data-aos="fade-up" className="bg-blue-300 p-5 rounded-lg">Box 1</div>
  <div data-aos="fade-down" className="bg-green-300 p-5 rounded-lg">Box 2</div>
  <div data-aos="flip-left" className="bg-red-300 p-5 rounded-lg">Box 3</div>
</div>
```

---

## 🔹 إعدادات إضافية ممكن تستخدمها

* `duration`: مدة الأنيميشن (بالملي ثانية).
* `delay`: تأخير قبل بدء الأنيميشن.
* `offset`: المسافة (px) من الـ viewport قبل بدء الأنيميشن.
* `once`: لو `true` → الأنيميشن يشتغل مرة واحدة فقط.

مثال:

```html
<div 
  data-aos="fade-up" 
  data-aos-delay="500" 
  data-aos-duration="1500" 
  data-aos-offset="200"
>
  Content
</div>
```

---

✅ الخلاصة:

1. نزّل المكتبة وثبّتها.
2. استدعي `AOS.init()` داخل `useEffect`.
3. ضيف `data-aos` على العناصر.
4. Tailwind + AOS يشتغلوا عادي مع بعض.

---

تحب أجهزلك **مثال جاهز** لصفحة كاملة (Landing Page صغيرة) مستخدم فيها Tailwind + AOS بحيث تشوف الشكل النهائي؟
